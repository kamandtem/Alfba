# چک‌لیست انتشار نسخه ۱٫۰٫۰

## قبل از انتشار

- [ ] `npm install` بدون خطا اجرا شود.
- [ ] `npm run release:check` سبز باشد.
- [ ] چهار GitHub Secret طبق فایل جداگانهٔ secrets ثبت شوند.
- [ ] نام مخزن و شاخهٔ اصلی درست باشد.
- [ ] یک tag با قالب `v1.0.0` ساخته و push شود.

## خروجی

- workflow باید با نام **Build Android Release APK** اجرا شود.
- از بخش Artifacts فایل `alfba-island-1.0.0.apk` و فایل `.sha256` دریافت شود.
- نصب APK روی حداقل یک دستگاه واقعی Android و تست صوت فارسی، چرخش صفحه، ذخیرهٔ پیشرفت و خروج از برنامه انجام شود.

کلید امضا را داخل Git یا داخل ZIP پروژه قرار ندهید. فقط فایل secrets را در محل امن نگه دارید.
