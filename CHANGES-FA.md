# تغییرات نسخهٔ ۵
- رفتن خودکار به تمرین بعد: بعد از درست انجام دادن هر تمرین، حدود ۲ ثانیه پیام آفرین می‌آید و خودش تمرین بعدی را می‌آورد:
  جمله‌سازی (جملهٔ بعد)، کلمه‌نویسی/تختهٔ مغناطیسی در «کلمه‌های درس» و «پیشنهاد کلمه» (کلمهٔ بعد)، بازی دوم و سوم دهکدهٔ اول (دور بعد)،
  فلش‌کارت (بعد از دیدن پشت کارت، کارت بعد)، بازی اول (مثل قبل)، بازی جدید «بخش‌بخش کن»، و بخش‌های قدیمی تخته حروف (تمرین با راهنما) و بازی کلمه‌ها.
  «نوشتن آزاد» عمداً خودکار جلو نمی‌رود. هر دکمهٔ «بعدی» دستی هم تایمر را لغو می‌کند.
- دهکدهٔ اول: به جای خانهٔ پنجم (خانهٔ درس‌ها) «بازی پنجم: بخش‌بخش کن» آمد (src/components/SyllableGame.tsx). انتخاب درس همچنان از دکمهٔ «نشانهٔ امروز» بالای نقشه است.
  - جدول سه‌طبقه مثل صفحه‌های ۴۸، ۵۰، ۵۵ و ۶۴ کتاب نگارش (یک جدول، نه دو جدول کنار هم):
    طبقهٔ ۱ خود کلمه با حرف‌های لمسی؛ کودک با کشیدن انگشت هر تعداد حرف را انتخاب می‌کند و در خانهٔ بخش در طبقهٔ ۲ می‌گذارد (زدن روی خانه یا کشیدن و رها کردن).
    طبقهٔ ۲ به تعداد هجاهای کلمه خانه دارد (۱، ۲ یا ۳ بخش). طبقهٔ ۳ برای هر صدا یک خانه دارد و هیچ خانه‌ای دو حرف نمی‌گیرد؛ اعراب ـَ ـِ ـُ خانهٔ خط‌تیره‌دار دارند.
  - قاعده‌های علمی هجای فارسی در src/utils/syllables.ts (هر هجا با یک صامت شروع می‌شود و یک مصوّت دارد؛ الگوهای CV / CVC / CVCC؛ «و/ی/ه» صامت یا مصوّت بسته به جایشان؛ «آ اَ اِ اُ او ای» اول کلمه یک خانه).
  - ۳ تا ۴ کلمه برای هر درس کتاب (بیشترشان همان کلمه‌های صفحه‌های کتاب)؛ فقط کلمه‌هایی که همهٔ نشانه‌هایشان تا درس انتخاب‌شده خوانده شده.
  - درس‌های ۱ تا ۳ کتاب فقط «صداهای هر کلمه را بنویس» (گردی‌ها، مثل صفحهٔ ۲۸، ۳۴، ۴۰، ۴۳) و از درس ۴ جدول کامل.
- نمادهای خانه و راهنما در نقشهٔ جزیرهٔ الفبا (و بالای نقشهٔ دهکده) بزرگ‌تر شدند (۸۰ پیکسل؛ گوشی‌های باریک ۷۰).

# تغییرات نسخهٔ ۴
- شکل نشانه‌ها مثل کتاب نگارش اول: «مـ» (گرد روی خط) و «هـ» (دوچشم) از glyphهای خود فونت تحریری (U+E103 و U+E104) در همهٔ نمایش‌های تک‌نشانه، جعبهٔ حروف و قطعه‌های کلمه‌سازی. متن کلمه‌ها دست نخورده (فقط نمایش).
- ترتیب نشانه‌ها مطابق جدول بودجه‌بندی کتاب نگارش ۱۴۰۴: درس ۶ «ای، ز»، درس ۷ «اِ ـه، ش»، درس ۱۷ «ص، ذ»، درس ۱۸ «ع، ث».
- بانک واژهٔ هر ۲۲ درس کتاب نگارش (src/data/wordBank.ts ← BOOK_RAW). تمرین‌های کلمه‌نویسی و «بازی اول» اول واژه‌های همان درس کتاب را می‌آورند؛ واژه‌ای که نشانهٔ نخوانده دارد خودکار عقب می‌افتد. چند جملهٔ کتاب هم به جمله‌سازی اضافه شد.
- نقشهٔ دهکده‌ها و خانه‌ها از بالا به پایین: دهکده/بازی اول بالاترین است و نقشه از بالا باز می‌شود. نقاشی‌ها و جای خانه‌ها همان است.
- پنجرهٔ منوی صفحهٔ شروع بزرگ‌تر شد (دفترچه و دکمه‌ها).
- توجه: چون ترتیب نشانه‌های ۱۱ تا ۱۴ و ۳۲ تا ۳۴ عوض شد، اگر کودک روی یکی از این درس‌ها بوده، یک بار درس را از «خانهٔ درس‌ها» دوباره انتخاب کنید.

# تغییرات نسخهٔ ۳
- صفحهٔ شروع دقیقاً مطابق تصویر مرجع: تابلو بالا وسط، بچه‌ها وسط، نوار سفید موج‌دار پایین با دکمهٔ منو (چپ)، پخش سبز بزرگ (وسط) و تنظیمات (راست). در تبلت/افقی کل صحنه کامل دیده می‌شود.
- دکمهٔ منو صفحهٔ دفترچهٔ فنری (Back.svg) را روی پس‌زمینهٔ آفتابی با پرتو، ابر و ستاره باز می‌کند:
  - پخش سبز ← نقشهٔ دهکده‌ها برای انتخاب نوع بازی
  - ادامه (cont) ← همان بخشی که کودک آخرین بار در آن بود (اسمش زیر دکمه نوشته می‌شود)
  - پیشرفت من (Pisraft) ← صفحهٔ جدید «پیشرفت من»: درصد کتاب، درس فعلی، ستاره/برگ/تمرین امروز، نوار هر دهکده و مسیر ۴۰ نشانه (یادگرفته / الان / بعداً)
  - تنظیمات ← پنجرهٔ تنظیمات
  - پرچم قرمز بالا ← بستن منو
- همهٔ دکمه‌های «تایید» با دکمهٔ سبز تیک‌دار و همهٔ ضربدرهای «بستن» با دکمهٔ قرمز جایگزین شد (public/assets/ui).
- شکل آغازین «بـ نـ مـ هـ» برای کلاس اول: از glyph‌های خود فونت تحریری استفاده شد (هیچ شکل موجودی تغییر نکرده، فقط ۴ کد اختصاصی U+E101..E104 اضافه شده):
  هـ دوچشم مثل کتاب، و نــ / بــ / مــ کمی کشیده‌تر. برای برگشت به حالت قبل: KID_INIT را در src/data/curriculum.ts خالی کنید.
- فایل‌های جدید: src/components/MyProgress.tsx، src/components/shared/ArtButtons.tsx، src/art-ui.css
بعد از دریافت: npm install
# تغییرات این نسخه
- نقشهٔ جزیره و دهکدهٔ حروف با نسبت ثابت تصویر مرجع؛ خانه‌ها و جزیره‌ها دقیقاً روی جای خودشان (مسیر از پایین شروع می‌شود).
- انیمیشن تکان‌خوردن/بالا‌پایین، نور زیر هر خانه، شماره و برچسب نام کودکانه روی هر خانه و جزیره، دست راهنما روی اولین مرحله.
- تصاویر نقشه به WebP سبک تبدیل شد تا اسکرول روان باشد؛ جای اسکرول بعد از برگشت حفظ می‌شود.
- فونت رابط کاربری: IRANSansDN  ← فایل IRANSansDN.ttf را در پوشهٔ public/fonts بگذارید.
- هدر کودکانهٔ همهٔ بازی‌ها، دکمهٔ برگشت فعال، انتخاب درس با پنجرهٔ کاشی‌های رنگی.
- دکمهٔ برگشت گوشی: یک صفحه به عقب برمی‌گردد و فقط از صفحهٔ شروع خارج می‌شود (پلاگین @capacitor/app).
- نوار وضعیت و نوار پایین اندروید: adjustMarginsForEdgeToEdge + @capacitor/status-bar + safe-area.
- بازی اول: نشانه‌ها همیشه کامل داخل کادر (مثل «آ»)، قبولی با ۷۰٪ پوشش خط‌چین، هشدار فوری وقتی کودک بیرون از خط می‌کشد، نوار پیشرفت.
- پیام آفرین/اشتباه: حباب رنگی با انیمیشن، ۱٫۵ تا ۲ ثانیه.
بعد از دریافت: npm install

## تختهٔ مغناطیسی (کلمه‌نویسی) – واژه‌های هر درس
- «کلمه‌های درس»: اول واژه‌های بخش «بنویس» زیر عنوان همان درس در کتاب نگارش (با نشانهٔ درس)، از کوتاه به بلند؛ بعد چند واژهٔ مهم دیگر همان درس و مرور درس قبل.
- «پیشنهاد کلمه»: واژه‌های بیرون از کتاب که نشانهٔ درس را دارند و فقط با نشانه‌های خوانده‌شده نوشته می‌شوند، به ترتیب از ساده به سخت (دیگر تصادفی نیست).
- «نوشتن آزاد» بدون تغییر.
- داده‌ها در `src/data/boardWords.ts`؛ هر واژه‌ای که نشانهٔ نخوانده داشته باشد خودکار حذف می‌شود.

## خواندن کلمات با موتور TTS بومی اندروید
- `speakPersian()` در `src/utils/audio.ts` حالا در APK اندروید از پلاگین `@capacitor-community/text-to-speech` (نسخهٔ 6.x، سازگار با Capacitor 7) و زبان `fa-IR` استفاده می‌کند.
- لمس‌های پشت‌سرهم: صدای قبلی فوراً قطع می‌شود (QueueStrategy.Flush) و صداها روی هم پخش نمی‌شوند.
- اگر TTS اندروید در دسترس نباشد، خطا بی‌صدا مدیریت می‌شود و برنامه crash نمی‌کند.
- در مرورگر، همان Web Speech API قبلی به‌عنوان جایگزین باقی مانده است.
- با قطع صدا (Mute) یا رفتن برنامه به پس‌زمینه، گفتار هم متوقف می‌شود.
- نیاز: روی گوشی یک موتور TTS با صدای فارسی نصب و فعال باشد (Settings › Text-to-speech output).

## دیکتهٔ شب (دهکدهٔ کلمه‌نویسی)
- دکمهٔ «پیشنهاد کلمه» شد «دیکته». با زدن آن دو گزینه می‌آید: «کلمهٔ کامل» (همان پیشنهاد کلمهٔ قبلی) و «کلمهٔ ناقص».
- کلمهٔ ناقص: ایموجیِ کلمه + کلمه با یک جای خالیِ نقطه‌چین به جای نشانهٔ درس (مثلاً درس «نـ ن»: سَمَ[...]د).
- کودک از جعبهٔ حروف (حروف خوانده‌شده پررنگ‌تر) شکل درست را می‌کشد و روی نقطه‌چین رها می‌کند؛ درست باشد مغناطیسی می‌چسبد، غلط باشد «غلط شد» اعلام می‌شود (اگر فقط شکل حرف اشتباه باشد، راهنمایی می‌کند).
- کلمه‌ها: کلمه‌های بخش «بنویس» همان درس کتاب نگارش + بقیهٔ کلمه‌های درس که نشانه را دارند + ۴ تا ۵ کلمهٔ بیرون از کتاب.
- از درس «نـ ن» به بعد فقط کلمه‌هایی که همهٔ نشانه‌هایشان پیش‌تر خوانده شده؛ در درس‌های آغازین، مثل خود کتاب، سخت‌گیری نمی‌شود.
- فایل‌های تغییرکرده: src/components/WordVillage.tsx، src/data/wordBank.ts، src/utils/pieces.ts (فقط افزودن تابع؛ خروجی توابع قبلی برای ۵۵۵ کلمه بررسی شد و عیناً یکسان است)، src/index.css (فقط سبک‌های تازه).
