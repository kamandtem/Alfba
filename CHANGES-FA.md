# تغییرات نسخهٔ ۴
- شکل نشانه‌ها مثل کتاب نگارش اول: «مـ» (گرد روی خط) و «هـ» (دوچشم) از glyphهای خود فونت تحریری (U+E103 و U+E104) در همهٔ نمایش‌های تک‌نشانه، جعبهٔ حروف و قطعه‌های کلمه‌سازی. متن کلمه‌ها دست نخورده (فقط نمایش).
- ترتیب نشانه‌ها مطابق جدول بودجه‌بندی کتاب نگارش ۱۴۰۴: درس ۶ «ای، ز»، درس ۷ «اِ ـه، ش»، درس ۱۷ «ص، ذ»، درس ۱۸ «ع، ث».
- بانک واژهٔ هر ۲۲ درس کتاب نگارش (src/data/wordBank.ts ← BOOK_RAW). تمرین‌های کلمه‌نویسی و «بازی اول» اول واژه‌های همان درس کتاب را می‌آورند؛ واژه‌ای که نشانهٔ نخوانده دارد خودکار عقب می‌افتد. چند جملهٔ کتاب هم به جمله‌سازی اضافه شد.
- نقشهٔ دهکده‌ها و خانه‌ها از بالا به پایین: دهکده/بازی اول بالاترین است و نقشه از بالا باز می‌شود. نقاشی‌ها و جای خانه‌ها همان است.
- پنجرهٔ منوی صفحهٔ شروع بزرگ‌تر شد (دفترچه و دکمه‌ها).
- توجه: چون ترتیب نشانه‌های ۱۱ تا ۱۴ و ۳۲ تا ۳۴ عوض شد، اگر کودک روی یکی از این درس‌ها بوده، یک بار درس را از «خانهٔ درس‌ها» دوباره انتخاب کنید.

# تغییرات نسخهٔ ۳
- صفحهٔ شروع دقیقاً مطابق تصویر مرجع: تابلو بالا وسط، بچه‌ها وسط، نوار سفید موج‌دار پایین با دکمهٔ منو (چپ)، پخش سبز بزرگ (وسط) و تنظیمات (راست). در تبلت/افقی کل صحنه کامل دیده می‌شود.
- دکمهٔ منو صفحهٔ دفترچهٔ فنری (Back.svg) را روی پس‌زمینهٔ آفتابی با پرتو، ابر و ستاره باز می‌کند:
  - پخش سبز ← نقشهٔ دهکده‌ها برای انتخاب نوع بازی
  - ادامه (cont) ← همان بخشی که کودک آخرین بار در آن بود (اسمش زیر دکمه نوشته می‌شود)
  - پیشرفت من (Pisraft) ← صفحهٔ جدید «پیشرفت من»: درصد کتاب، درس فعلی، ستاره/برگ/تمرین امروز، نوار هر دهکده و مسیر ۴۰ نشانه (یادگرفته / الان / بعداً)
  - تنظیمات ← پنجرهٔ تنظیمات
  - پرچم قرمز بالا ← بستن منو
- همهٔ دکمه‌های «تایید» با دکمهٔ سبز تیک‌دار و همهٔ ضربدرهای «بستن» با دکمهٔ قرمز جایگزین شد (public/assets/ui).
- شکل آغازین «بـ نـ مـ هـ» برای کلاس اول: از glyph‌های خود فونت تحریری استفاده شد (هیچ شکل موجودی تغییر نکرده، فقط ۴ کد اختصاصی U+E101..E104 اضافه شده):
  هـ دوچشم مثل کتاب، و نــ / بــ / مــ کمی کشیده‌تر. برای برگشت به حالت قبل: KID_INIT را در src/data/curriculum.ts خالی کنید.
- فایل‌های جدید: src/components/MyProgress.tsx، src/components/shared/ArtButtons.tsx، src/art-ui.css
بعد از دریافت: npm install
# تغییرات این نسخه
- نقشهٔ جزیره و دهکدهٔ حروف با نسبت ثابت تصویر مرجع؛ خانه‌ها و جزیره‌ها دقیقاً روی جای خودشان (مسیر از پایین شروع می‌شود).
- انیمیشن تکان‌خوردن/بالا‌پایین، نور زیر هر خانه، شماره و برچسب نام کودکانه روی هر خانه و جزیره، دست راهنما روی اولین مرحله.
- تصاویر نقشه به WebP سبک تبدیل شد تا اسکرول روان باشد؛ جای اسکرول بعد از برگشت حفظ می‌شود.
- فونت رابط کاربری: IRANSansDN  ← فایل IRANSansDN.ttf را در پوشهٔ public/fonts بگذارید.
- هدر کودکانهٔ همهٔ بازی‌ها، دکمهٔ برگشت فعال، انتخاب درس با پنجرهٔ کاشی‌های رنگی.
- دکمهٔ برگشت گوشی: یک صفحه به عقب برمی‌گردد و فقط از صفحهٔ شروع خارج می‌شود (پلاگین @capacitor/app).
- نوار وضعیت و نوار پایین اندروید: adjustMarginsForEdgeToEdge + @capacitor/status-bar + safe-area.
- بازی اول: نشانه‌ها همیشه کامل داخل کادر (مثل «آ»)، قبولی با ۷۰٪ پوشش خط‌چین، هشدار فوری وقتی کودک بیرون از خط می‌کشد، نوار پیشرفت.
- پیام آفرین/اشتباه: حباب رنگی با انیمیشن، ۱٫۵ تا ۲ ثانیه.
بعد از دریافت: npm install
