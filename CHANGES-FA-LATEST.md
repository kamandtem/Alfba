# نسخهٔ ۹: رفع به‌هم‌ریختگی‌ها

- «بـ نـ تـ» در انتخاب درس، جعبهٔ حروف، ترکیب‌ها و همهٔ بازی‌های دهکدهٔ اول با شکل آغازیِ خود فونت نمایش داده می‌شوند (glyph کشیدهٔ U+E101/E102/E105 دیگر استفاده نمی‌شود). «مـ» و «هـ» کتابی دست نخورده‌اند.
- باگ «__paper__»: در بازی‌های دیگر (چی مثل چی، فلش‌کارت، بخش‌بخش، تختهٔ حروف، پیام‌های آفرین) متن خام نماد جایگزین نمایش داده می‌شد. حالا همه‌جا یا ایموجی واقعی است یا آیکون صفحهٔ خالی (public/assets/word-placeholder.svg).
- «چی مثل چی» و فلش‌کارت فقط واژه‌هایی را می‌آورند که تصویر واقعی دارند.
- حدود ۸۰ واژهٔ بی‌تصویر ایموجی مناسب گرفتند (FALLBACK_EMOJI در wordBank.ts)؛ واژه‌های انتزاعی آیکون صفحهٔ خالی می‌گیرند.
- صفحهٔ شروع: تابلوی آویزان دوباره دقیقاً وسط است (انیمیشن قدیمیِ خود تصویر آن را نصف عرض به کنار می‌برد). عنوان «دهکده الفبا» با فونت تحریری نوشته می‌شود، نه فونت سیستمی.
- پنجرهٔ «میخوای بری؟»: قاب SVG بریده شد تا پنل وسط صفحه باشد؛ دکمه‌های آره/نه هم‌اندازه، هم‌ارتفاع و قرینه نسبت به وسط پنل (انیمیشن ورود دیگر جای دکمه‌ها را خراب نمی‌کند).

# نسخهٔ ۱۰
- صفحهٔ شروع: نوشتهٔ اضافهٔ «دهکده الفبا» حذف شد؛ خود SVG تابلو اسم را دارد.
- بخش‌بخش کن: کلمهٔ طبقهٔ اول خودکار تا عرض ردیف بزرگ می‌شود (۶۴ تا ۲۰۰ پیکسل) و ردیف بلندتر شد.
- انتخاب هوشمند هجا: لمس هر جای طبقهٔ اول، هجای همان ناحیه را کامل انتخاب می‌کند (راست = هجای اول، وسط = وسط، چپ = آخر). کشیدن انگشت هجای کناری را کامل اضافه می‌کند. هجای گذاشته‌شده دوباره انتخاب نمی‌شود. نمایش تغییری نکرده.
- ترکیبات: «بـ تـ نـ» با شکل خود فونت؛ ترکیب‌ها بدون کشیده (با، بو، بی) و اعراب روی خود حرف (بَـ) تا زیر حرف نیفتد.

# نسخهٔ ۱۱
- بخش‌بخش کن: کلمهٔ طبقهٔ اول کوچک‌تر (۴۴ تا ۱۱۶px) و با فضای کافی بالای کلمه؛ سرکش «ک» و «گ» دیگر بریده نمی‌شود (در خانه‌ها و مهره‌ها هم).
- طبقهٔ سوم و مهره‌ها: هر حرف مطابق نوشتار کلمه ولی بدون اتصال به حرف قبل: زنگ ← ز نـ گ ، سمند ← سـ مـ نـ د. فقط ه/ع/غ شکل میانی خودشان را دارند (ـهـ ـعـ ـغـ).
- ترکیبات: اعراب (ـَ ـِ ـُ) جداگانه و دقیق بالای/زیر حرف رسم می‌شود؛ فتحه دیگر زیر «سـ» نمی‌افتد.
- جمله‌سازی: نوشتهٔ پایین صفحه حذف شد؛ «جملهٔ بعدی» کنار دکمهٔ تیک. پایین صفحه صندوقچهٔ «جمله‌های من»: جملهٔ درست پرواز می‌کند داخلش و با لمس، فهرست جمله‌های ساخته‌شده باز می‌شود.
- جعبهٔ حروف: پنجرهٔ شکل‌ها بدون نوار اسکرول و با سرکش کامل ک/گ. ترتیب شکل‌های «ه» همه‌جا مثل انتخاب درس (هـ ـهـ ـه ه)؛ روی دکمهٔ جعبه و برچسب درس هم درست شد.
- دیکتهٔ کلمهٔ ناقص: سه نقطهٔ بالایی حذف شد؛ فقط یک نقطه‌چین طلایی.
- موسیقی: از اول نصب پخش می‌شود (قبلاً لمس pointerdown در گوشی مجوز پخش نمی‌داد). بلندی پیش‌فرض پایین (۰٫۳).
- پایان دور: در بازی اول تا پنجم، فلش‌کارت، کلمه‌نویسی (درس و دیکته) و جمله‌سازی بعد از آخرین مورد پیام «آفرین! موفق شدی!» با دکمهٔ «یک دور دیگر» می‌آید؛ دیگر بی‌خبر به مورد اول برنمی‌گردد. (بازی دوم ۳ مرحله یا به تعداد شکل‌ها، بازی سوم ۳ مرحله)
